
// hex ids
export const Idtext = {
	from: (hex: string) => hex,
	to: (hex: string) => hex,
}

// // base58 ids
// import {Base58, Hex} from "@benev/slate"
// export const Idtext = {
// 	from: (hex: string) => Base58.string(Hex.bytes(hex)),
// 	to: (text: string) => Hex.string(Base58.bytes(text)),
// }

